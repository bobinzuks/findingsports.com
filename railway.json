{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": [
      "mockup/backend/**"
    ],
    "buildCommand": "cd mockup/backend && cp package-minimal.json package.json && npm install --production --no-audit --no-fund"
  },
  "deploy": {
    "startCommand": "cd mockup/backend && node server-emergency.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  },
  "environments": {
    "production": {
      "NODE_ENV": "production"
    }
  }
}