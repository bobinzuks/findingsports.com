[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
# Skip npm install - our failsafe server uses no dependencies
cmds = ["echo 'No dependencies to install - using absolute failsafe server'"]

[start]
cmd = "cd mockup/backend && node absolute-failsafe-server.js"