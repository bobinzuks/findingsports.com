# Railway-optimized requirements for Finding Sports Venue Harvester
# Minimal dependencies for small Railway instances

# Core web framework
Flask==2.3.3
Werkzeug==2.3.7

# HTTP client for web scraping
aiohttp==3.9.1

# HTML parsing
beautifulsoup4==4.12.2
lxml==4.9.3

# Railway deployment
gunicorn==21.2.0

# Note: sqlite3, json, re, asyncio are built into Python
# Total size: ~15MB (vs 500MB+ for AI version)